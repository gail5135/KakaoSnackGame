doctype html
html
	head
		meta(charset='utf-8')
		title KingCat-Phaser3
		style(type='text/css').
			body {
				background: #000000;
				padding: 0px;
				margin: 0px;
			}
			canvas {
				display: block;
				margin: 0;
				position: absolute;
				top: 50%;
				left: 50%;
				transform: translate(-50%, -50%);
			}
		script(src='js/phaser.min.js')
		script(src='js/game.js')
		script(type='text/javascript').
			var user_info = !{JSON.stringify(user_info)}
			var ranking = !{JSON.stringify(ranking)}
	body